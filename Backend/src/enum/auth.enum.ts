export enum Role {
  User = "user",
  Superadmin = "superadmin",
}

export enum Gender {
  Male = "male",
  Female = "female",
  Other = "other",
}

export enum Status {
  Active = "active",
  InActive = "inActive",
}
